#include <iostream>

int main()
{
	std::cout << "Enter two numbers." << std::endl;
	


	int (a);
	int (b);
	std::cin >> (a) >> (b);

	if (a > b)
	{
		std::cout << (a) << std::endl;
		std::cin.get();
	}

	else
	{
		std::cout << (b) << std::endl;
		std::cin.get();
	}
	return 0;
}
