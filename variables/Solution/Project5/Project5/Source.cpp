#include <iostream>

int main()
{
	int a;
	int b;
	int c;
	int d;
	int e;

	int f;
	int g;
	int h;
	int i;
	int j;

	std::cout << "Hey user. Enter five random numbers." << std::endl; //Intructing the user.

	std::cin >> (a);  //Aquiring information. 

	std::cin >> (b);

	std::cin >> (c);

	std::cin >> (d);

	std::cin >> (e);
	std::cin.get();

	if (a < b, c, d, e)//ordering information
	{
		f = a;
	}

	else if (b < a, c, d, e)
	{
		f = b;
	}

	else if (b < a, c, d, e)
	{
		f = b;
	}

	else if (b < a, c, d, e)
	{
		f = b;
	}

	else(b < a, c, d, e)
	{
		f = b;
	}

	std::cout << int(a) << std::endl;
	
	std::cin.get();
	return 0;
}